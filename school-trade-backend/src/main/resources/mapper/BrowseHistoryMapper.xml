<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shanzhu.st.mapper.BrowseHistoryMapper">
    <insert id="insertBrowseHistory">
        INSERT INTO user_browse_history (user_id, idle_id, create_time)
        VALUES (#{userId}, #{idleId}, NOW())
    </insert>
</mapper>